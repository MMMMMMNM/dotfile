# ⚡️ Requirement

1. a Nerd Font (optional)
2. Git(clone support)
3. paru(install aur)

# 🚀 Getting Started
*Use bash to run*
```bash
git clone https://github.com/MMMMMMNM/dotfile
#then run isntall.sh to install basic setting
./install
```
